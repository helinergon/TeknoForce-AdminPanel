@page
@model TeknoForce.Pages.Products.IndexModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Ürünler";
    Layout = "_AdminLayout";
}
<div class="card shadow-sm">
    <div class="card-body">
        <h3 class="mb-2">@ViewData["Title"]</h3>
        <p class="text-muted">
            Bu sayfa yakında aktif olacak.
        </p>
    </div>
</div>